# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Wake On Lan' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Wake On Lan
  pod 'AnyFormatKit'
  pod 'Alamofire'
  pod "PlainPing"
  pod 'PKHUD', '~> 5.0'
end
